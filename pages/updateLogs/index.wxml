<view class="logs-warp">

  <view class="logs-item" wx:for="{{logs}}" wx:key="{{index}}">
    <view class="logs-item-title">{{item.version}} {{item.date}}</view>
    <view class="logs-item-content">
      <text>{{item.content}}</text>
    </view>
  </view>
  
</view>